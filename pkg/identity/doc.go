// Package identity provides a Bun-backed user, password, and session module
// with Huma HTTP handlers. The package owns identity only; OAuth, SSH keys,
// authorization, auditing, and application data remain host concerns.
package identity
