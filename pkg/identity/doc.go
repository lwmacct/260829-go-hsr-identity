// Package identity provides a Bun-backed user, password, and session module
// with Huma HTTP handlers. It also owns the generic human-challenge contract
// and optional authentication enforcement; concrete providers remain host
// concerns. OAuth, SSH keys, auditing, and application data remain host
// concerns.
package identity
